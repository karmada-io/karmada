<!--
AUTO-GENERATED by hack/agent-skills/scripts/extract_crd_schema.py.
Do not edit by hand. Re-run the script after CRD changes:
  python3 hack/agent-skills/scripts/extract_crd_schema.py \
      --crd <path-to-crd> --out <this-file> --title <Kind>
-->

# PropagationPolicy — Schema Reference

Each row below is one field path in the CRD's OpenAPI v3 schema. The agent
should treat this file as authoritative when picking field names, types, and
enum values. Descriptions are copied verbatim from the CRD.

| Field path | Type | Required | Description |
|------------|------|----------|-------------|
| `apiVersion` | `string` | no | APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors... |
| `kind` | `string` | no | Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/... |
| `metadata` | `object` | no | — |
| `spec` | `object` | yes | Spec represents the desired behavior of PropagationPolicy. |
| `spec.activationPreference` | `string (enum: Lazy)` | no | ActivationPreference indicates how the referencing resource template will be propagated, in case of policy changes.  If empty, the resource template will respond to policy changes immediately, in other words, any policy changes will driv... |
| `spec.association` | `boolean` | no | Association tells if relevant resources should be selected automatically. e.g. a ConfigMap referred by a Deployment. default false. Deprecated: in favor of PropagateDeps. |
| `spec.conflictResolution` | `string (enum: Abort, Overwrite) (default: Abort)` | no | ConflictResolution declares how potential conflict should be handled when a resource that is being propagated already exists in the target cluster.  It defaults to "Abort" which means stop propagating to avoid unexpected overwrites. The... |
| `spec.dependentOverrides` | `array<string>` | no | DependentOverrides represents the list of overrides(OverridePolicy) which must present before the current PropagationPolicy takes effect.  It used to explicitly specify overrides which current PropagationPolicy rely on. A typical scenari... |
| `spec.failover` | `object` | no | Failover indicates how Karmada migrates applications in case of failures. If this value is nil, failover is disabled. |
| `spec.failover.application` | `object` | no | Application indicates failover behaviors in case of application failure. If this value is nil, failover is disabled. If set, the PropagateDeps should be true so that the dependencies could be migrated along with the application. |
| `spec.failover.application.decisionConditions` | `object` | yes | DecisionConditions indicates the decision conditions of performing the failover process. Only when all conditions are met can the failover process be performed. Currently, DecisionConditions includes several conditions: - TolerationSecon... |
| `spec.failover.application.gracePeriodSeconds` | `integer` | no | GracePeriodSeconds is the maximum waiting duration in seconds before application on the migrated cluster should be deleted. Required only when PurgeMode is "Graciously" and defaults to 600s. If the application on the new cluster cannot r... |
| `spec.failover.application.purgeMode` | `string (enum: Directly, Gracefully, Never, Immediately, Graciously) (default: Gracefully)` | no | PurgeMode represents how to deal with the legacy applications on the cluster from which the application is migrated. Valid options are "Directly", "Gracefully", "Never", "Immediately"(deprecated), and "Graciously"(deprecated). Defaults t... |
| `spec.failover.application.statePreservation` | `object` | no | StatePreservation defines the policy for preserving and restoring state data during failover events for stateful applications.  When an application fails over from one cluster to another, this policy enables the extraction of critical da... |
| `spec.failover.cluster` | `object` | no | Cluster indicates failover behaviors in case of cluster failure. If this value is nil, the failover behavior in case of cluster failure will be controlled by the controller's no-execute-taint-eviction-purge-mode parameter. If set, the fa... |
| `spec.failover.cluster.purgeMode` | `string (enum: Directly, Gracefully) (default: Gracefully)` | no | PurgeMode represents how to deal with the legacy applications on the cluster from which the application is migrated. Valid options are "Directly", "Gracefully". Defaults to "Gracefully". |
| `spec.failover.cluster.statePreservation` | `object` | no | StatePreservation defines the policy for preserving and restoring state data during failover events for stateful applications.  When an application fails over from one cluster to another, this policy enables the extraction of critical da... |
| `spec.placement` | `object` | no | Placement represents the rule for select clusters to propagate resources. |
| `spec.placement.clusterAffinities` | `array<object>` | no | ClusterAffinities represents scheduling restrictions to multiple cluster groups that indicated by ClusterAffinityTerm.  The scheduler will evaluate these groups one by one in the order they appear in the spec, the group that does not sat... |
| `spec.placement.clusterAffinities[].affinityName` | `string` | yes | AffinityName is the name of the cluster group. |
| `spec.placement.clusterAffinities[].clusterNames` | `array<string>` | no | ClusterNames is the list of clusters to be selected. |
| `spec.placement.clusterAffinities[].exclude` | `array<string>` | no | ExcludedClusters is the list of clusters to be ignored. |
| `spec.placement.clusterAffinities[].fieldSelector` | `object` | no | FieldSelector is a filter to select member clusters by fields. The key(field) of the match expression should be 'provider', 'region', or 'zone', and the operator of the match expression should be 'In' or 'NotIn'. If non-nil and non-empty... |
| `spec.placement.clusterAffinities[].labelSelector` | `object` | no | LabelSelector is a filter to select member clusters by labels. If non-nil and non-empty, only the clusters match this filter will be selected. |
| `spec.placement.clusterAffinities[].overflowAffinities` | `array<object>` | no | OverflowAffinities defines additional cluster groups that the scheduler can progressively include when the primary group (defined by the inline ClusterAffinity) has insufficient resources. Groups are expanded in order and contracted in r... |
| `spec.placement.clusterAffinity` | `object` | no | ClusterAffinity represents scheduling restrictions to a certain set of clusters. Note:   1. ClusterAffinity can not co-exist with ClusterAffinities.   2. If both ClusterAffinity and ClusterAffinities are not set, any cluster      can be... |
| `spec.placement.clusterAffinity.clusterNames` | `array<string>` | no | ClusterNames is the list of clusters to be selected. |
| `spec.placement.clusterAffinity.exclude` | `array<string>` | no | ExcludedClusters is the list of clusters to be ignored. |
| `spec.placement.clusterAffinity.fieldSelector` | `object` | no | FieldSelector is a filter to select member clusters by fields. The key(field) of the match expression should be 'provider', 'region', or 'zone', and the operator of the match expression should be 'In' or 'NotIn'. If non-nil and non-empty... |
| `spec.placement.clusterAffinity.labelSelector` | `object` | no | LabelSelector is a filter to select member clusters by labels. If non-nil and non-empty, only the clusters match this filter will be selected. |
| `spec.placement.clusterTolerations` | `array<object>` | no | ClusterTolerations represents the tolerations. |
| `spec.placement.clusterTolerations[].effect` | `string` | no | Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute. |
| `spec.placement.clusterTolerations[].key` | `string` | no | Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys. |
| `spec.placement.clusterTolerations[].operator` | `string` | no | Operator represents a key's relationship to the value. Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal. Exists is equivalent to wildcard for value, so that a pod can tolerate all taints of a particular category. Lt and G... |
| `spec.placement.clusterTolerations[].tolerationSeconds` | `integer` | no | TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Ze... |
| `spec.placement.clusterTolerations[].value` | `string` | no | Value is the taint value the toleration matches to. If the operator is Exists, the value should be empty, otherwise just a regular string. |
| `spec.placement.replicaScheduling` | `object` | no | ReplicaScheduling represents the scheduling policy on dealing with the number of replicas when propagating resources that have replicas in spec (e.g. deployments, statefulsets) to member clusters. |
| `spec.placement.replicaScheduling.replicaDivisionPreference` | `string (enum: Aggregated, Weighted)` | no | ReplicaDivisionPreference determines how the replicas is divided when ReplicaSchedulingType is "Divided". Valid options are Aggregated and Weighted. "Aggregated" divides replicas into clusters as few as possible, while respecting cluster... |
| `spec.placement.replicaScheduling.replicaSchedulingType` | `string (enum: Duplicated, Divided) (default: Divided)` | no | ReplicaSchedulingType determines how the replicas is scheduled when karmada propagating a resource. Valid options are Duplicated and Divided. "Duplicated" duplicates the same replicas to each candidate member cluster from resource. "Divi... |
| `spec.placement.replicaScheduling.weightPreference` | `object` | no | WeightPreference describes weight for each cluster or for each group of cluster If ReplicaDivisionPreference is set to "Weighted", and WeightPreference is not set, scheduler will weight all clusters the same. |
| `spec.placement.spreadConstraints` | `array<object>` | no | SpreadConstraints represents a list of the scheduling constraints. |
| `spec.placement.spreadConstraints[].maxGroups` | `integer` | no | MaxGroups restricts the maximum number of cluster groups to be selected. |
| `spec.placement.spreadConstraints[].minGroups` | `integer` | no | MinGroups restricts the minimum number of cluster groups to be selected. Defaults to 1. |
| `spec.placement.spreadConstraints[].spreadByField` | `string (enum: cluster, region, zone, provider)` | no | SpreadByField represents the fields on Karmada cluster API used for dynamically grouping member clusters into different groups. Resources will be spread among different cluster groups. Available fields for spreading are: cluster, region,... |
| `spec.placement.spreadConstraints[].spreadByLabel` | `string` | no | SpreadByLabel represents the label key used for grouping member clusters into different groups. Resources will be spread among different cluster groups. SpreadByLabel should not co-exist with SpreadByField. |
| `spec.placement.workloadAffinity` | `object` | no | WorkloadAffinity represents inter-workload affinity and anti-affinity scheduling policies. |
| `spec.placement.workloadAffinity.affinity` | `object` | no | Affinity represents inter-workload affinity scheduling rules. These are hard requirements: workloads will only be scheduled to clusters that satisfy the affinity term if it is specified.  For the first workload of an affinity group (when... |
| `spec.placement.workloadAffinity.antiAffinity` | `object` | no | AntiAffinity represents inter-workload anti-affinity scheduling rules. These are hard requirements: workloads will be scheduled to avoid clusters where matching workloads are already scheduled. |
| `spec.preemption` | `string (enum: Always, Never) (default: Never)` | no | Preemption declares the behaviors for preempting. Valid options are "Always" and "Never". |
| `spec.preserveResourcesOnDeletion` | `boolean` | no | PreserveResourcesOnDeletion controls whether resources should be preserved on the member clusters when the resource template is deleted. If set to true, resources will be preserved on the member clusters. Default is false, which means re... |
| `spec.priority` | `integer (default: 0)` | no | Priority indicates the importance of a policy(PropagationPolicy or ClusterPropagationPolicy). A policy will be applied for the matched resource templates if there is no other policies with higher priority at the point of the resource tem... |
| `spec.propagateDeps` | `boolean` | no | PropagateDeps tells if relevant resources should be propagated automatically. Take 'Deployment' which referencing 'ConfigMap' and 'Secret' as an example, when 'propagateDeps' is 'true', the referencing resources could be omitted(for savi... |
| `spec.resourceSelectors` | `array<object>` | yes | ResourceSelectors used to select resources. Nil or empty selector is not allowed and doesn't mean match all kinds of resources for security concerns that sensitive resources(like Secret) might be accidentally propagated. |
| `spec.resourceSelectors[].apiVersion` | `string` | yes | APIVersion represents the API version of the target resources. |
| `spec.resourceSelectors[].kind` | `string` | yes | Kind represents the Kind of the target resources. |
| `spec.resourceSelectors[].labelSelector` | `object` | no | A label query over a set of resources. If name is not empty, labelSelector will be ignored. |
| `spec.resourceSelectors[].labelSelector.matchExpressions` | `array<object>` | no | matchExpressions is a list of label selector requirements. The requirements are ANDed. |
| `spec.resourceSelectors[].labelSelector.matchLabels` | `object` | no | matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requi... |
| `spec.resourceSelectors[].name` | `string` | no | Name of the target resource. Default is empty, which means selecting all resources. |
| `spec.resourceSelectors[].namespace` | `string` | no | Namespace of the target resource. Default is empty, which means inherit from the parent object scope. |
| `spec.schedulePriority` | `object` | no | SchedulePriority defines how Karmada should resolve the priority and preemption policy for workload scheduling.  This setting is useful for controlling the scheduling behavior of offline workloads. By setting a higher or lower priority,... |
| `spec.schedulePriority.priorityClassName` | `string` | yes | PriorityClassName specifies which PriorityClass to use. Its behavior depends on PriorityClassSource:  Behavior of PriorityClassName:  For KubePriorityClass: - When specified: Uses the named Kubernetes PriorityClass.  For PodPriorityClass... |
| `spec.schedulePriority.priorityClassSource` | `string (enum: KubePriorityClass)` | yes | PriorityClassSource specifies where Karmada should look for the PriorityClass definition. Available options: - KubePriorityClass: Uses Kubernetes PriorityClass (scheduling.k8s.io/v1) - PodPriorityClass: Uses PriorityClassName from PodTem... |
| `spec.schedulerName` | `string (default: default-scheduler)` | no | SchedulerName represents which scheduler to proceed the scheduling. If specified, the policy will be dispatched by specified scheduler. If not specified, the policy will be dispatched by default scheduler. |
| `spec.suspension` | `object` | no | Suspension declares the policy for suspending different aspects of propagation. nil means no suspension. no default values. |
| `spec.suspension.dispatching` | `boolean` | no | Dispatching controls whether dispatching should be suspended. nil means not suspend, no default value, only accepts 'true'. Note: true means stop propagating to all clusters. Can not co-exist with DispatchingOnClusters which is used to s... |
| `spec.suspension.dispatchingOnClusters` | `object` | no | DispatchingOnClusters declares a list of clusters to which the dispatching should be suspended. Note: Can not co-exist with Dispatching which is used to suspend all. |
| `spec.suspension.dispatchingOnClusters.clusterNames` | `array<string>` | no | ClusterNames is the list of clusters to be selected. |
