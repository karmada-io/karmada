apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  annotations: {}
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - image: nginx:alpine
          name: nginx
status: {}
